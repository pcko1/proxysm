# UI revamp, "Blocks" (chosen look)

Sources of the Claude Design canvas page "Blocks (chosen)" at
https://claude.ai/artifact/Nq6UX9GRaPk5zPnfs9YADq (private to the owner).
Each `Blocks*.dc.html` is one artboard; `canvas.json` is the full canvas index
(it also lists the v1 and direction-study boards, whose v1 sources are in `../ui-revamp-v1/`).
Sample data only. Spec: `docs/superpowers/specs/2026-09-20-ui-revamp-blocks-design.md`.

Boards: `BlocksNav` (sidebar), `BlocksOverview`, `BlocksProxies`, `BlocksRequests`,
`BlocksProjects`, `BlocksPalette`. There is no Pools board; Pools follows the Projects pattern.
